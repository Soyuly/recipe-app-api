version: '"3.9"'

services:
  app:
    build:
      context: . # 작업 현재 디렉터리
      args:
        - DEV=true
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app #실시간으로 업데이트사항이 반영되게 한다
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"